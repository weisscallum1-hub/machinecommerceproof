# Tests

`npm test` runs the isolated Node self-test in a temporary directory. It creates signing keys there, exercises receipt creation, listing, lookup, independent verification, chain verification, statistics and tamper detection, then deletes the temporary state.

No test private keys are stored in the repository.
